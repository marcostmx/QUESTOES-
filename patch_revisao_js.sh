#!/bin/bash
sed -i 's/function lerSRS()    { var s = ler(SRS_KEY, {}); Object.keys(s).forEach(function(k) { var m = k.split("|")[0]; if (m === "null" || m.indexOf("revis") > -1 || m.indexOf("simulado") > -1) delete s\[k\]; }); return s; }/function lerSRS() { var s = ler(SRS_KEY, {}); var flat = {}; Object.keys(s).forEach(function(m) { if (m === "null" || m.indexOf("revis") > -1 || m.indexOf("simulado") > -1) return; var obj = s[m]; if (typeof obj === "object") { Object.keys(obj).forEach(function(idx) { var qData = obj[idx]; if (qData && (qData.nextReview || qData.due)) { flat[m + "|" + idx] = { due: qData.nextReview || qData.due, level: qData.level || 0 }; } }); } else if (m.indexOf("|") > -1) { flat[m] = s[m]; } }); return flat; }/g' public/revisao-espacada.js
